# PlayHT API Credentials
PLAYHT_SECRET_KEY=your_secret_key_here
PLAYHT_USER_ID=your_user_id_here

# Server Configuration
PORT=3001

# Environment
# Set to 'production' when deploying, 'development' for local development
NODE_ENV=production

# CORS Configuration
# Add both your Digital Ocean URL and custom domain
ALLOWED_ORIGIN=https://audiomax-jo3yc.ondigitalocean.app,https://audiomax.ai

# For complete setup, you might want all these URLs:
# ALLOWED_ORIGIN=https://audiomax-jo3yc.ondigitalocean.app,https://audiomax.ai,http://localhost:5173,http://localhost:5174

# Note: Make sure to include both the Digital Ocean URL and your custom domain
# to ensure the app works during the domain transition and after it
